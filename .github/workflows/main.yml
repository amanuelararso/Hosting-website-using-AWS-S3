name: Hello World Workflow

on: 
  push:
    branches: [ master ]
  
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello World"
        shell: bash

  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Goodbye World
        run: echo "Goodbye World"
        shell: bash
